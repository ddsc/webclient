Lizard.Models.GraphItem = Backbone.Model.extend({
    defaults: {
        timeseries: null,
        color: null
    }
    // ,initialize: function() {
    //     this.set({
    //         'color': colorbrewer.Set3[9][Math.round(Math.random()*8+1)]
    //     });
    // }
});
